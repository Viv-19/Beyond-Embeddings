# Validators

Request validation schemas (e.g., Joi, Zod, or express-validator).

Currently empty — input validation is handled inline in controllers.
As the API grows, validation logic will be extracted into reusable schemas here.
