# Storage

File storage adapters (local disk, AWS S3, Google Cloud Storage).

Currently using local disk storage via Multer (configured in middlewares/uploadMiddleware.js).
This directory will hold cloud storage adapters when the app is deployed to production.
