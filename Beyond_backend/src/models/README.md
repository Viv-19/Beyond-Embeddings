# Models

Data models and Prisma-related helpers.

Note: The primary data models are defined in `prisma/schema.prisma`.
This directory is for additional model logic (e.g., computed fields, model helpers).
